# Deployment to GitHub Pages

Цей проект налаштований для автоматичного деплою на GitHub Pages через GitHub Actions.

## Налаштування

### 1. Перевірте GitHub репозиторій налаштування

1. Перейдіть на GitHub до вашого репозиторію
2. Відкрийте **Settings** → **Pages**
3. У розділі "Source", вибіраємо:
   - **Deploy from a branch**
   - **Branch**: `main` 
   - **Folder**: `/ (root)` (якщо GitHub Pages деплойтьме з root) або перевіримо автоматичні налаштування

### 2. Налаштування GitHub Actions

GitHub Actions workflow вже налаштований в `.github/workflows/deploy.yml`

Workflow автоматично:
- Запускається при push на `main` гілку
- Встановлює Node.js 20
- Встановлює залежності (`npm ci`)
- Будує проект (`npm run build`)
- Експортує статичні файли в папку `out/`
- Деплойть на GitHub Pages

### 3. Запуск деплою

Просто зробіть push на `main` гілку:

```bash
git add .
git commit -m "Deploy documentation to GitHub Pages"
git push origin main
```

GitHub Actions автоматично запустить build та деплойту.

### 4. Перевірка статусу деплою

1. Перейдіть в репозиторій на GitHub
2. Клікніть на вкладку **Actions**
3. Подивіться статус workflow "Deploy Next.js site to Pages"

Статус буде:
- 🟡 **Running** - деплой в процесі
- 🟢 **Success** - деплой завершено успішно
- 🔴 **Failed** - виникла помилка

### 5. Доступ до сайту

Після успішного деплою, сайт буде доступний за адресою:

```
https://<ваш-username>.github.io/<назва-репозиторія>/
```

Наприклад, якщо ваш username `worze` та репозиторій `avro-mkdocs-2`:

```
https://worze.github.io/avro-mkdocs-2/
```

## Що робить workflow

### Build Step
- Визначає package manager (npm, yarn)
- Налаштовує Node.js 20
- Встановлює залежності через `npm ci`
- Будує проект через `npm run build`
- Генерує статичні файли в папці `out/`

### Deployment Step
- Завантажує статичні файли як GitHub Pages artifact
- Деплойть файли на GitHub Pages

## Локальна перевірка

Щоб перевірити, що сайт правильно будується локально:

```bash
npm run build
# Будуть створені файли в папці `out/`
```

Щоб повірити статичний сайт локально:

```bash
npx http-server out
# Сайт буде доступний на http://localhost:8080
```

## Тригери для деплою

Workflow запускається автоматично при:
1. **Push на main гілку** - основний тригер
2. **Manual trigger** - через Actions → "Deploy Next.js site to Pages" → "Run workflow"

## Розробка

Під час розробки використовуйте:

```bash
npm run dev
# Запустить dev сервер на http://localhost:3000
```

Сайт автоматично оновлюватиметься при змінах файлів.

## Структура проекту

- `/content/` - Markdown файли для документації
- `/src/` - React компоненти та Next.js маршрути
- `/public/` - Статичні файли (іконки, зображення)
- `/out/` - Експортовані статичні HTML файли (генерується при `npm run build`)
- `.github/workflows/deploy.yml` - GitHub Actions workflow конфігурація
